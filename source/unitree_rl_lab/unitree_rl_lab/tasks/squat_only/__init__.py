"""Periodic squat task (G1 29DOF).

Standalone task package: place this whole folder under
  source/unitree_rl_lab/unitree_rl_lab/tasks/<your_name>/

and it will be picked up automatically by the ``import_packages`` call in
``tasks/__init__.py``.

You can freely rename the top-level folder (e.g. ``squat_only``,
``whole_body``, ``periodic_squat``, ...). Nothing outside this folder needs to
change.
"""
